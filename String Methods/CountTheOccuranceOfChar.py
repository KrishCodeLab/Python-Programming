# Count occurrences of char in string  using count().
s=input("Enter String : ")
char=(input("Enter Alpha to count the occurance of it in string "))

print(f"count of {char} in {s} is = {s.count(char)}")